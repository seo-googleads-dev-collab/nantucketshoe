# Public URL where Strapi is reachable from the browser / build
NEXT_PUBLIC_STRAPI_URL=http://localhost:1337

# Optional: Strapi API token (needed only if you restrict public read access)
# NEXT_PUBLIC_STRAPI_API_TOKEN=
